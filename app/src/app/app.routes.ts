import {Routes} from '@angular/router'

/**
 * Application route definitions.
 *
 * Currently empty — the dashboard is rendered directly by the
 * root component.
 */
export const routes: Routes = []
